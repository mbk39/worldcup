# Local editor / preview config (not for sharing)
.claude/

__pycache__/
*.pyc
.venv/
venv/
# Local data & secrets — never commit
predictions.db
predictions.json
predictions.json.imported
worldcup.db
secret_key.txt
*.tmp
